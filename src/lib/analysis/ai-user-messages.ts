/** User-facing copy when the structured Gemini report cannot be produced or parsed. */
export const USER_SAFE_AI_REPORT_UNAVAILABLE =
  "We couldn't generate the AI summary right now, but your lease highlights are still available below.";
